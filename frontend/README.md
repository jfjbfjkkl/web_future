# Next.js (migration depuis Vite/React)

- App Router dans `app/`
- Le rendu UI/CSS/animations est conservé en réutilisant `src/App.tsx`
- Routes:
  - `/` (home)
  - `/login`
  - `/free-fire`

## Dev
```bash
npm install
npm run dev
```

## Build (Vercel)
```bash
npm run build
npm run start
```
